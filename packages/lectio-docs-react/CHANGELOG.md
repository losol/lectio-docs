# @eventuras/lectio-docs-react

## 0.1.1

### Patch Changes

- bfa837a: `<Search>` now renders an alert when the search provider fails, using the
  `error` state `useDocsSearch` already exposes — a broken index is no longer
  indistinguishable from a query with no matches.
- Updated dependencies [3b48fff]
  - @eventuras/lectio-docs@0.3.0
