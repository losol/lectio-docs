---
title: Quickstart
description: The one-command docs site.
---

# Quickstart

Run `npx lectio build` and get a static, searchable docs site — no framework
wiring, no config beyond `docs.config.mjs`.

Drop markdown into `content/`. Every file becomes a page; the frontmatter
`title` names it in the navigation.
